<h1>Have a look on all the offers we have</h1>
<ul>
  <% @offers.each do |offer| %>
    <li><%= link_to offer.title, offer_path(offer) %></li>
  <% end %>
</ul>
<%= link_to 'New Offer', new_offer_path %>